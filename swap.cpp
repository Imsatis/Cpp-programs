#include<iostream>

using namespace std;
  int main()
{
  int a,b,r;
  cout<<"Enter The no = ";cin>>a;
  cout<<"Enter The no = ";cin>>b;
 
  swap(a,b);
            
  cout<<"\na is = "<<a;
  cout<<"\nb is = "<<b;

} 

  swap(int &x,int &y)
  { 
   
   
     }
